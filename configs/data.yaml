# YOLO Dataset Configuration — LLVIP Fused
# Auto-generated by Cell 4 of the training notebook.
# Update paths to match your local or Kaggle working directory.

path: /kaggle/working/llvip_fusion_yolov8   # Dataset root
train: images/train                          # Training images (relative to path)
val: images/val                              # Validation images (relative to path)

nc: 1                                        # Number of classes
names:
  - person
